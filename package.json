{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "init": "npm install && cd client && npm install && cd ../server && npm install",
    "client": "cd ./client && HOST=0.0.0.0 npm start",
    "server": "cd ./server && nodemon --watch",
    "start": "npm-run-all --parallel client server",
    "dev": "npm start"
  },
  "keywords": [],
  "author": "Angel Ramirez",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "git": "^0.1.5",
    "nodemon": "^3.0.1",
    "npm-run-all": "^4.1.5"
  }
}